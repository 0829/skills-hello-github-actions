{
    "name": "node-test-minimal",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "test": "vitest run --coverage.enabled true"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^3.2.4",
        "vitest": "^3.2.4"
    }
}
